# Top-level .gitignore for a monorepo structure (client/server)

# Node.js dependencies and build outputs
# ------------------------------------
# Node modules for both client and server
**/node_modules/

# pnpm-specific files
# pnpm's virtual store and lockfile.
# You generally WANT to commit pnpm-lock.yaml to ensure consistent installs across environments.
# However, if you have a global virtual store, you might ignore .pnpm.
# For typical project setup, node_modules/ will already cover the virtual store.
.pnpm-debug.log*
.pnpm-store/ # If pnpm creates a store directly in your project root (less common)

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Optional npm cache directory
.npm/

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
# Keep .env.example if you provide one for setup instructions, but ignore actual .env files.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local # For any specific local environment files

# VS Code specific files (IDE settings)
.vscode/

# Build artifacts (for both client and server)
# -------------------------------------------
# Client-side (Vite/React) build output
client/dist/

# Server-side (if you compile your server code)
server/dist/
server/build/

# IDE and OS generated files
# ---------------------------
.idea/             # IntelliJ IDEA files
.DS_Store          # macOS specific
Thumbs.db          # Windows specific
Desktop.ini        # Windows specific

# Test coverage results
coverage/
.nyc_output/

# TypeScript build info (if you were using TypeScript for server)
*.tsbuildinfo

# For Shadcn UI (if you generated components)
# Any temporary files generated by Shadcn or build tools might go here.
# Usually, Shadcn components are committed, as they are part of your source.
# If you have a specific build output or cache from Shadcn, add it here.
# For example, if you have a .shadcn-cache/ folder:
# .shadcn-cache/

# Misc
# ----
# Crash reports
*.stackdump

# VSCode Live Share
.vsls/